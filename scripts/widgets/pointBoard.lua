local pointBoard = require "widgets/image"
local UIAnim = require "widgets/uianim"


local pointBoard = Class(pointBoard, function(self, atlas, tex, default_tex)
    pointBoard._ctor(self, nil,nil)
end)


return pointBoard
